export enum ProtectedRotes {
  HOME = "/home",
  USERS = "/users",
  PROFILE = "/profile",
  PROJECTS = "/projects",
}

export enum PublicRotes {
  SIGNIN = "/signin",
  SIGNUP = "/signup",
  NOT_FOUND = "/404",
}
