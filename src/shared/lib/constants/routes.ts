export enum PROTECTED_ROUTES {
	PRODUCTS = "/products",
	USERS = "/users",
	PROFILE = "/profile",
	PROJECTS = "/projects",
}

export enum PUBLIC_ROUTES {
	SIGNIN = "/signin",
	SIGNUP = "/signup",
	NOT_FOUND = "/404",
}
